const activo=true;

/*
let mensaje='';

 if ( mensaje ) {

     mensaje='activo';

 } else {

    mensaje='inactivo';

 }
*/
/*

condicionales ternarios
const mensaje = ( activo ) ? 'activo' : 'inactivo';
const mensaje2 = ( activo ) ? 'activo' : null;
*/

const mensaje = activo  && 'Activo'

 console.log(mensaje);


 //condicional if a ternario
